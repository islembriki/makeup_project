{% if field.value %}
    <img src="{{ asset('images/products/' ~ field.value) }}" 
         style="max-height: 100px;"
         class="img-thumbnail"
         alt="{{ entity.instance.name }}">
{% else %}
    <span class="badge bg-secondary">No image</span>
{% endif %}